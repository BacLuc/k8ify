package targetconfigs

var (
	ExposePlainLoadBalancerSchemeKey = "exposePlainLoadBalancerScheme"
	AppuioCloudscale                 = "appuio-cloudscale"

	// SecurityContextKey is the x-targetCfg key under which pod-level
	// security-context defaults live (pod-level subset only).
	SecurityContextKey = "securityContext"
)
